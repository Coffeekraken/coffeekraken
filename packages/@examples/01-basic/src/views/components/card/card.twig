<div class="card">
    <h1 class="s-typo:h1 s-mbe:30">{{ title|raw }}</h1>
    <p class="s-typo:lead s-mbe:20 s-tc:accent">{{ intro|raw }}</p>
    <p class="s-typo:p">{{ description|raw }}</p>
    <a class="s-btn s-mbs:40 s-color:accent" href="{{ cta.link.url }}" target="{{ cta.link.target }}">
        {{ cta.label }}
    </a>
</div>
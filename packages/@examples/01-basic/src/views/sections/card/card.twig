<section {{ __id(id) }} class="section-card {{ __spacesClasses(spaces, frontspec) }}">
    {{ __nodeDataTemplate('views.sections.card', _context)|raw }}
    <div class="s-container:wide">
        <div class="s-layout">
            {% if cardOne %}
                <div>
                    {% include 'sugar/components/card/card.twig' with cardOne only %}
                </div>
            {% endif %}
            {% if cardTwo %}
                <div>
                    {% include 'sugar/components/card/card.twig' with cardTwo only %}
                </div>
            {% endif %}
        </div>
    </div>
</section>
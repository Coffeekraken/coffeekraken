<section class="section-tabs">
    <div class="s-container">
        <div class="s-layout:1222">

            <div>
                <div class="__tabs s-tab:vertical">
                    {% for tab in tabs %}
                        <div>
                            {{ tab.title }}
                        </div>
                    {% endfor %}
                </div>
            </div>

            {# {% for spec in specs %}
                <div class="__spec">
                    <h1 class="s-typo:h1 s-mbe:30">{{ spec.title|raw }}</h1>
                    <p class="s-typo:lead s-mbe:20 s-tc:accent">{{ spec.intro|raw }}</p>
                    <p class="s-typo:p">{{ spec.description|raw }}</p>
                    <a class="s-btn s-mbs:40 s-color:accent" href="{{ spec.cta.link.url }}" target="{{ spec.cta.link.target }}">
                        {{ spec.cta.label }}
                    </a>
                </div>
            {% endfor %} #}
        </div>
    </div>
</section>
<section class="section-slider" s-appear in="bottom" delay="300">
    <div class="s-container">
        <s-slider nav controls lnf="default-tight">
            {% for slide in slides %}
                <div s-slider-slide class="__slide">
                    <img src="{{ slide.image.url }}" alt="{{ slide.image.alt }}" title="{{ slide.image.title }}" />
                </div>
            {% endfor %}
        </s-slider>
    </div>
</section>
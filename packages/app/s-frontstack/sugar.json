{
  "config": {
    "folders": [
      {
        "scope": "default",
        "path": "./src/config"
      }
    ]
  },
  "cli": [
    {
      "stack": "frontstack",
      "description": "Sugar Frontstack application that allows you to develop quickly and efficiently",
      "defaultAction": "exec",
      "actions": {
        "exec": {
          "description": "Exec the frontstack application into the current working directory",
          "process": "./src/node/exec/exec.cli.js"
        }
      }
    }
  ]
}

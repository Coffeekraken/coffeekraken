import __SValidator from '../SValidator';

describe('@coffeekraken/s-validator', () => {
    it('should instanciate and use simple "min" validator correctly', () => {
        const validator = new __SValidator();
        // const res = validator.validate('Hello World Plop', {
        //     min: 20,
        // });
        expect(true).toBe(true);
    });
});

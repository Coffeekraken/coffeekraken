export default {
  /**
   * @name               scope
   * @namespace           config.docblock
   * @type               String
   * @default             [config.scope.prefix]
   *
   * Specify a scope "prefix" used to prefix all the "classes" generated through
   * a docblock renderer.
   *
   * @since           2.0.0
   * @author         Olivier Bossel <olivier.bossel@gmail.com> (https://olivierbossel.com)
   */
  scope: '[config.scope.prefix]'
};

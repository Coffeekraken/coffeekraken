// @shared

import __SInterface from '../../interface/SInterface';

class LogInterface extends __SInterface {
  static definition = {
    value: {
      require: true
    }
  };
}
export = LogInterface;

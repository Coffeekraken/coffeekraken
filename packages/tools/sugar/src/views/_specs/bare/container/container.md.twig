{% extends 'sugar/readme/layouts/doc/doc.twig' %}
{% block body %}

<!--
/**
 * @name            Container
 * @namespace       specs.views.bare.container
 * @type            Markdown
 * @platform        blade
 * @platform        twig
 * @status          stable
 * @menu            Specs / Bare           /specs/sugar.views.bare.container
 *
 * @since           2.0.0
 * @author    Olivier Bossel <olivier.bossel@gmail.com> (https://coffeekraken.io)
 */
-->

{% set specs = __readSpec('sugar.views.bare.container') %}
{{ __specsToMarkdown(specs, {
    features: {
        type: {
            title: 'Type',
            description: 'The container type you want. **' ~ containers|join(', ') ~ '**' 
        }   
    }   
})|raw }}

{% endblock %}

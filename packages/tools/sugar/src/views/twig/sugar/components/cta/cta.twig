<a class="s-cta s-btn s-color:{{ color.value }} {{ attributes.class }}" {{ __attrs(attributes, ['class'])|raw }}
    href="{{ link.url }}"
    {% if link.newWindow %} target="_blank" {% endif %}
    color="{{ color.value }}"
>
    {{ label ? label : link.text }}
</a>    
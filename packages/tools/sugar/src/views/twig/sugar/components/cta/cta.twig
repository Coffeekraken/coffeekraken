<a class="s-cta s-btn s-color:{{ color }} {{ attributes.class }}" {{ __attrs(attributes, ['class'])|raw }}
    href="{{ link.url }}"
    target="{{ link.target }}"
    color="{{ color }}"
>
    {{ label ? label : link.text }}
</a>
<div class="s-layout_area-{{ areaId }}
    {{ __classes({
        spaces: spaces
    }) }}
    {{ attributes.class }}
    {{ additionalClasses }}
" {{ __attrs(attributes, ['class'])|raw }}>
    {{ __nodeDataTemplate('sugar.views.bare.layoutArea', _context)|raw }}
    {{ html|raw }}
</div>